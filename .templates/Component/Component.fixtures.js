export const noDataFixture = {
};

export const basicDataFixture = {
  loadingKeys: new Set()
};

export const loadingFixture = {
  loadingKeys: new Set().add('default')
};

export const mockFnFixture = {
  onAction: () => {}
};
